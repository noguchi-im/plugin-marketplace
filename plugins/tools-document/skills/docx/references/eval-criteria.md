# docx 評価基準

本ファイルは SPEC.md の受け入れ基準から自動生成されたユーザ向け検証チェックリストである。
スキルの動作品質を評価・改善する際に使用する。

## 知識の網羅性

- [ ] Given テキスト抽出が必要な時
      When ガイドを参照する
      Then pandoc による変換方法と、XML 展開によるアクセス方法が得られる

- [ ] Given 新規 Word 文書を作成する時
      When ガイドを参照する
      Then docx-js による文書生成のコード例（段落、表、画像、ヘッダー/フッター）が得られる

- [ ] Given ページ設定（用紙サイズ、マージン、横向き）が必要な時
      When ガイドを参照する
      Then DXA 単位の一覧表と設定コード例が得られる

- [ ] Given リストを作成する時
      When ガイドを参照する
      Then LevelFormat.BULLET / DECIMAL による正しい方法が得られ、Unicode 弾丸文字の禁止が明記されている

- [ ] Given テーブルを作成する時
      When ガイドを参照する
      Then dual width 設定（columnWidths + cell width）の必須ルールとコード例が得られる

- [ ] Given 既存文書を編集する時
      When ガイドを参照する
      Then 展開 → XML 編集 → 検証 → 再パッキングのワークフローが得られる

- [ ] Given 変更履歴・コメントを操作する時
      When XML リファレンスを参照する
      Then 挿入・削除マーカー、コメント XML の記述方法が得られる

- [ ] Given 日本語 Word 文書を扱う時
      When 日本語リファレンスを参照する
      Then 日本語フォント指定と行間設定の方法が得られる

## 環境セットアップ

- [ ] Given docx-js が未インストールの環境で新規作成を指示された時
      When 事前チェックで docx-js が見つからない
      Then 自動インストールを実行し、成功すれば処理を続行する

- [ ] Given 自動インストールが失敗した時（権限不足等）
      When インストールコマンドの実行がエラーになる
      Then 必要なコマンドとインストール手順がユーザーに提示される

- [ ] Given 依存が不在だがフォールバック可能な時
      When フォールバック表に代替手段がある
      Then 代替手段で処理を続行する（例: pandoc なし → XML 直読み）

- [ ] Given LibreOffice が未インストールの環境で変換を試みた時
      When スクリプトが外部コマンドの存在を確認する
      Then 未インストールのコマンド名とインストール方法が表示される

## 既存文書編集パイプライン

- [ ] Given .docx ファイルがある時
      When 展開スクリプトを実行する
      Then XML が展開・整形され、隣接する変更履歴と Run が統合される

- [ ] Given 展開済み XML がある時
      When 検証スクリプトを実行する
      Then スキーマ違反と自動修復可能な問題が報告される

- [ ] Given 展開済み XML がある時
      When 再パッキングスクリプトを実行する
      Then 検証・自動修復を経た .docx ファイルが生成される

- [ ] Given 変更履歴を含む .docx がある時
      When 変更承認スクリプトを実行する
      Then 全変更が承認されたクリーンな .docx が生成される

## コメント操作

- [ ] Given 展開済み文書がある時
      When コメント追加スクリプトでコメントを追加する
      Then コメント XML が生成され、マーカー配置指示が出力される

- [ ] Given 展開済み文書がある時
      When コメント追加スクリプトで返信を追加する
      Then 既存コメントへの返信が追加される

## 新規作成

- [ ] Given docx-js で文書を生成した時
      When 検証スクリプトで検証する
      Then スキーマ準拠が確認できる（エラーがあれば修正指針が得られる）

## スクリプトのポータビリティ

- [ ] Given スクリプトを引数なしで実行した時
      When 引数不足が検出される
      Then usage メッセージと使用例が表示される

- [ ] Given スクリプトに `--help` を渡した時
      When ヘルプが生成される
      Then 各引数の説明が表示される

- [ ] Given スクリプトをモジュールとして import した時
      When メイン処理が実行されない
      Then 関数のみが利用可能になる
