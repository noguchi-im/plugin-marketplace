# xlsx 評価基準

本ファイルは SPEC.md の受け入れ基準から自動生成されたユーザ向け検証チェックリストである。
スキルの動作品質を評価・改善する際に使用する。

## 知識の網羅性

- [ ] Given データ分析が必要な時
      When ガイドを参照する
      Then pandas による読み込み・分析方法が得られる

- [ ] Given 新規 Excel を作成する時
      When ガイドを参照する
      Then openpyxl によるワークブック構築のコード例が得られる

- [ ] Given 数式を含む Excel を作成する時
      When ガイドを参照する
      Then 数式の文字列記述方法と再計算スクリプトによる再計算手順が得られる

- [ ] Given セルのフォーマットを設定する時
      When フォーマットリファレンスを参照する
      Then 数値フォーマット文字列と色コード規約が得られる

- [ ] Given 財務モデルを作成する時
      When フォーマットリファレンスを参照する
      Then 財務モデルの色コード規約と数値形式ルールが得られる

- [ ] Given CSV/TSV を Excel に変換する時
      When ガイドを参照する
      Then pandas による読み込みと openpyxl への変換方法が得られる

## 環境セットアップ

- [ ] Given openpyxl が未インストールの環境で Excel 操作を指示された時
      When 事前チェックで import が失敗する
      Then pip install を自動実行し、成功すれば処理を続行する

- [ ] Given 自動インストールが失敗した時
      When インストールコマンドの実行がエラーになる
      Then 必要なコマンドとインストール手順がユーザーに提示される

- [ ] Given LibreOffice が未インストールの環境で数式再計算を試みた時
      When 再計算スクリプトが LibreOffice の存在を確認する
      Then 未インストールのコマンド名とインストール方法が表示される

## 数式再計算パイプライン

- [ ] Given 数式を含む .xlsx がある時
      When 再計算スクリプトを実行する
      Then LibreOffice が数式を再計算し、結果を JSON で返す

- [ ] Given 再計算後に数式エラーがある時
      When エラーが検出される
      Then エラー種類、件数、セル位置を含む JSON レポートが出力される

- [ ] Given 再計算後にエラーがない時
      When チェックが完了する
      Then status: "success" の JSON が返される

## スクリプトのポータビリティ

- [ ] Given スクリプトを引数なしで実行した時
      When 引数不足が検出される
      Then usage メッセージと使用例が表示される

- [ ] Given スクリプトに `--help` を渡した時
      When ヘルプが生成される
      Then 各引数の説明が表示される
